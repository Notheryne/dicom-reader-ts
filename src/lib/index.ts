import { readBytes, readFile } from './file-reader';
import { getPixelData } from './pixel-data';
import {
  createTag,
  getTagInfo,
  getTagsGroup,
  getTagValue,
} from './tag-helpers';

export {
  createTag,
  getPixelData,
  getTagInfo,
  getTagValue,
  getTagsGroup,
  readBytes,
  readFile,
};
